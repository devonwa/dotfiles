# hyprpaper v0.8.x uses hyprlang's side-data syntax.
# `*` matches all monitors; paths expand `~` (not $HOME).
wallpaper[*]:path = ~/dotfiles/system/nixos/wallpaper.png

# hide the hyprpaper logo / splash quote overlay
splash = false